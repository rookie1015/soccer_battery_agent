# 更新记录

## 2026-09-10 v0.5.4：旧复盘奖金自动补算

- 打开奖金日历或点击刷新时，自动读取手机内旧版复盘保存的赛果与原始票面，补算十四场和任九中奖注数。
- 自动查询中国体彩网官方单注奖金并写回旧历史记录；奖金尚未公布时保留待更新状态，后续刷新会继续查询。
- 缺少新版 `prize` 字段不再要求重新复盘；只有同时缺失完整票面或赛果的记录无法补算。
- Android 升级为 `0.5.4`（18），Python 包版本同步为 `0.5.4`。

## 2026-09-07 v0.5.2：复盘输入与金额展示

- 复盘期号改为输入即保存，再次进入复盘页或重启 App 后恢复上次输入。
- 任九复盘在期号右侧显示本单金额；对旧历史复盘记录，根据任九保留场次的票面选项补算金额。
- Android 升级为 `0.5.2`（16），Python 包版本同步为 `0.5.2`。

## 2026-09-07 v0.5.1：十四场预算覆盖优化

- 十四场动态规划改为优先最大化最终概率下的整票联合覆盖率，强制单选数仅作为同覆盖率时的次级取舍，仍保留风险标记。
- 每场开放概率最高的单选、双选及全包，允许扩大原始模型建议；原建议已在预算内时也执行优化。使用最终概率，不在预算层二次加平局分数。
- 增加固定选择数量的局部扰动审计：在两项之间转移 0.5/1.0 个百分点，统计换选场景数，保存到 `budget_stability` 及逐场依据；不添加未经验证的稳定性惩罚或平局权重。
- 保留原始建议及预算删项，扩大覆盖的解释改为“预算分配”；恢复历史记录时不会套用新票面的敏感性审计。
- 默认 50 元预算容差、任九优化目标和单选复盘统计口径保持现状。旧对冲分支的显式单平仍被锁定。
- Android 升级为 `0.5.1`（15）。全量测试 `249 passed`；7 期快照、2 档预算的 14 组技术回放中，理论覆盖率均不低于基线，任九票面一致。这不是严格截止前的效果验证。
- Android API 36.1 模拟器通过本机引擎、26119/26110 快照回放、十四场与任九切换、敏感性提示、历史恢复和冷启动验证；未发现 App FATAL、Python traceback 或 ANR。

## 2026-09-04 v0.5.0：拟合式数学模型与平局独立校准

- 生产模型升级为 `market-residual-1x2-v5`，包含 v3 的证据归属去重、v4 的拟合式 Dixon-Coles，以及 v5 的平局独立校准。
- 平局校准使用平赔变化、亚洲浅盘、大小球水平和大小球降盘形成有上限的市场结构残差；Dixon-Coles 低比分概率与联赛平局率只控制残差可信度，避免重复计权。
- 平局校准的特征、可用性、逐项贡献、胜平负修正和文字证据均写入预测与移动 API，便于逐场审计。
- 严格走步实验同步拟合并保存平局校准系数；旧模型版本的实验和激活参数不会进入 v5。
- 当前 v5 实验共识别 56 场历史样本，其中严格赛前样本 0 场、探索样本 42 场、排除样本 14 场；未达到训练 84 场、独立测试 84 场和至少 6 个测试期之前保持 `collecting`，不自动晋级。
- Android 版本升级为 `0.5.0`（versionCode `14`）。

## 2026-09-03 未发布：拟合式 Dixon-Coles

- 数学层从固定联赛进球率、固定主场优势和近期均值升级为正则化 Dixon-Coles/Poisson 拟合。
- 新快照保存两队逐场历史，并按匹配联赛补充当前及上一赛季的完整比赛池；训练记录包含日期、对手、主客场、比分、可用 xG 和中立场标记。
- 模型联合估计联赛进球环境、联赛主场优势、球队进攻强度和防守脆弱度；历史比赛按 120 天半衰期衰减，友谊赛额外降权，小样本向全局与联赛先验收缩。
- 低比分相关参数 `rho` 根据历史 0:0、0:1、1:0、1:1 结构拟合；中立场不使用主场优势，次回合情境继续作为独立赛制修正，避免重复计权。
- 数学审计新增逐场样本数、时间衰减后有效样本、联赛进球率、主场系数和 `rho`；逐场数据不足或球队身份未匹配时安全回退旧聚合模型。
- 模型版本升级为 `market-residual-1x2-v4`，旧快照只进入探索轨道，不能为新模型晋级。

## 2026-09-02 未发布：模型口径统一与去重计权

- App、生产预测与候选实验统一使用当前模型版本；历史报告不再丢失结构化模型状态。
- 市场、基本面与 Dixon-Coles 只融合一次；投注第二选项和预算压缩直接使用最终综合概率，不再重新累计同一批证据。
- 有真实 xG/进失球时，近期状态、低比分和平局率归 Dixon-Coles 所有，基本面层保留原值用于审计但不再重复修正。
- 伤停与赛程只由基本面层处理；Dixon-Coles 不再二次调整 xG，当前阵容评分和缺席惩罚也不再重复伤停影响。
- 删除融合后的独立主客场胜率修正；其近期比赛样本已经进入实力与数学层。
- 模型架构升级为 `market-residual-1x2-v3`，旧版本实验和激活参数不会被新模型误用。

## 2026-08-29 v0.4.0 市场锚定与滚动候选模型

- 胜平负概率改为以去水市场概率为基准，基本面与独立数学证据只做有上限的对数概率修正。
- 基本面新增状态、战意、伤停、赛程、平局结构的来源可靠度审计；默认值只表示缺失，不再冒充真实中性证据。
- 新增可滚动拟合的基本面内部系数，使用严格赛前快照、按期 walk-forward 和正则收缩生成候选模型。
- Dixon-Coles 缺少 xG/进失球时的状态兜底不再参与市场场次的数学修正，避免与基本面重复计权。
- 三选保护优先于完整分析第二选项，证据排序不能把受保护三选强制压成二选。
- 手机复盘只刷新候选实验，不再自动激活生产模型；生产参数需要通过赔率基线门禁并由用户显式确认。
- 报告与移动 API 增加基本面特征、可靠度、修正量、预算利用率和预算删平审计。
- Android 升级为 `0.4.0`（versionCode `12`），Python 与 iOS 营销版本同步为 `0.4.0`。

## 2026-08-23 v0.3.5 Android 网络容错修复

- 修复 Android 无法连接 DBpedia 球队别名服务时，Java `SocketTimeoutException` 未被 Python 网络异常处理捕获、进而中止整份分析的问题。
- DBpedia 只用于可选的中英文球队别名增强；服务超时后现在跳过该增强并继续使用已有别名和赛程上下文完成分析。
- Android 本机保存最近一次原始 Python 分析堆栈，便于后续区分数据源、存储和报告生成故障。
- 修复把预算内若干独立单式线路的赛果并集合并展示为一张复式票、导致页面多选范围与显示金额不一致的问题。
- 14 场恢复正规复式计价：各场“预算票面”选择数相乘得到注数，再按每注 2 元计价；“模型建议”不参与出票金额计算。
- 任九改为独立优化：共享赛前基础概率，但单独联合选择 9 场及每场单选、双选或全包，不再按十四场模型建议覆盖率简单排序。
- 任九使用独立正规复式计价和独立预算上限；报告与 Android 展示任九实际票面、注数、成本和理论联合覆盖率，并明确两种玩法同时购买时金额需要相加。
- 修复仅凭联赛和开球时间猜测球队主客方向造成的身份污染；旧身份库中与内置核验身份冲突的 kickoff-only 别名会自动清理。
- 修复主源赔率与期次专属辅助赔率呈主客镜像时仍继续采用错误方向的问题；镜像自动纠正，其他严重冲突切换到核验赔率。
- 版本保持 `0.3.5`（versionCode `11`），重新构建同版本修复包。

## 2026-08-22 v0.3.4 精简预算组合展示

- Android 不再向用户展开数百条完整14场线路，改为只显示预算、注数、多平覆盖和平局配额摘要；完整线路继续作为内部预算优化和复盘数据保留。
- 飞书通知不再附带全部线路清单，避免1000元预算产生500行难以阅读的消息。
- 用户文案由“多平独立线路”调整为“预算组合”，明确“500注”是预算内组合数量，不是500场单选建议。
- Android 升级为 `0.3.4`（versionCode `10`），保留 `v0.3.3` 回退标签。

## 2026-08-22 v0.3.3 多平线路历史修复

- 修复 Android 从历史分析列表重新打开 v0.3.2 报告时没有恢复 `line_portfolio`，导致成本回退为旧矩形复式乘积的问题；26109 的1000元报告不再错误显示为419904元。
- 历史 Markdown 解析现在只读取“14场逐场建议”表，不再把后续“平局线路配额”表误当成额外比赛。
- 历史报告恢复预算、独立线路总数、完整线路、每场平局目标/实际配额、多平线路数及两两同线最低数，Android 历史卡片与刚生成时保持一致。
- 模拟器重新生成并验证26109、26110各自的500元和1000元报告；Android 升级为 `0.3.3`（versionCode `9`），保留 `v0.3.2` 回退标签。

## 2026-08-22 v0.3.2 多平独立线路组合

- 废除“只选一场平局做独立对冲”的实际执行路径，预算改为若干条完整且互不重复的14场线路，每2元一条；500元预算可生成250条线路。
- 模型原始建议中每个被保留的平局都按相对概率获得线路配额，组合器同时优先提高多平同线数量和任意两个候选平局共同出现的最低覆盖数。
- API、Markdown、HTML、Android 和 iOS 新增独立线路总数、每场平局目标/实际配额、多平线路数、候选平局两两同线最低数及完整线路清单。
- 复盘新增最佳线路命中场数、完整命中状态，以及实际平局组合同时出现在线路中的数量；不再用单场对冲是否命中代替整条线路验收。
- 26108 严格快照在500元预算下生成250条唯一线路，12个模型候选平局全部达到配额，133条线路至少含两个候选平局，任意候选平局对至少共同出现33条，实际三个平局第4/5/11场共同出现24条；最佳线路命中11/14。
- 使用本地12个完整期次和2、10、50、128、500、2000元六档预算做72组压力回放，全部满足不超预算、线路不重复、每个平局实际配额不低于目标；当预算买完全部唯一组合时，配额按有限组合空间的数学上限调整。
- 26108 未出现14/14并非漏选实际平局：模型原始建议在第13场只保留胜/平而实际为负，组合器不会越过模型边界凭空加入该赛果。该限制在报告和发布记录中明确保留。
- Android 升级为 `0.3.2`（versionCode `8`），iOS 构建号升级为 `4`，Python 包同步为 `0.3.2`；保留 `v0.3.0` 与 `v0.3.1` 标签用于回退。

## 2026-08-22 v0.3.1 预算内平局对冲

- 自动分析不再执行 v0.3.0 的“整票强制单平”；改为主票与独立平局对冲分支共享同一预算，主票保留非平路径，对冲分支只固定一场单选平。
- 只有模型原始覆盖平局、主票因预算删除平局、真实市场平局概率达标且至少有一项可信结构支持时才生成对冲；没有候选时不强行设置单平。
- 对冲默认最多分配总预算的 20%；候选排序以去水市场概率为锚，数学与赛前结构只负责准入，不重复累计相关信号；异常偏低的派生总进球不作为支持。
- API、Markdown、HTML、Android 和 iOS 展示主票成本、对冲场次、对冲完整票面与组合总成本；复盘新增候选单平和整个对冲分支的命中记录。
- 26108 严格快照在 500 元上限回放中生成主票 384 元、对冲第 5 场单平 96 元，共 480 元，且不再把任何比赛的模型建议改写成战术单平。
- 复盘把预算压缩漏判进一步拆分出“预算删平导致漏判”，用于区分模型原始漏平与票面成本取舍；26108 期严格快照复盘确认，500 元档有 3 场、1000 元档有 2 场属于预算删平。
- 新增500网、中国足彩网逐期辅助核验：正常情况下不覆盖新浪，新浪欧赔缺失时才使用辅助源均值补缺，并在每场快照保存来源与匹配结果。
- 新增可选 football-data.org 免费 Token，在覆盖范围内补充核验赛程时间和比赛状态；API-Football 与 X 暂不接入。
- Android 分析只保留完整分析流程，删除关键资料网络故障后的简单分析自动降级。
- 完整分析无法继续时不生成报告或历史记录，App 直接展示按网络、数据、期号或本机存储分类的中文错误。
- Python 桥接层统一清理异常信息，不向用户显示 Python 异常类型、堆栈、内部路径或原始错误代码。

## 2026-08-15 v0.3.0 市场锚定与战术单平

- 预测概率改为以真实去水市场概率为锚点；没有有效结构化信息时，信息层权重自动归零，不再让中性默认值改变结论。
- Dixon-Coles 数学层按实际数据质量动态降权；缺少球队 xG/进失球样本时只保留极低权重先验，并把释放出的权重交还市场。
- 常规信息与数学融合层对市场的单项修正限制在 12 个百分点内，避免孤立数据或短期状态造成失控偏移；杯赛次回合等明确比赛结构继续使用独立保护。
- 新增每期最多一场“战术单平”：必须同时具备真实市场、质量达标的数学平局信号，以及赔率结构或低比分/平局倾向信息支持。
- 战术单平明确标为高风险主动博取，与 60% 门槛的稳胆单选、预算强制单选分别统计和展示。
- API、Markdown、HTML、Android 和 iOS 增加战术单平标记；Android 升级为 `0.3.0`（versionCode `6`），iOS 与 Python 包同步为 `0.3.0`。
- Android 分析运行期间按钮切换为“取消分析”；取消后立即恢复操作，并通过协作式取消令牌阻止后台继续生成报告或写入历史记录。

## 2026-08-10 假单选与冷平防守修正

- 单选默认门槛由最高概率 52% / 领先 14 个百分点收紧到 60% / 领先 20 个百分点，减少高波动场次被过早当成胆材。
- 预算优化优先最小化低置信度、数据不完整或与 Dixon-Coles 方向冲突的强制单选，再比较整票覆盖概率。
- 永久保留预算压缩前的模型建议；Android、iOS、HTML、Markdown 和 API 分开显示“模型建议”与“预算票面”，强制单选不再冒充模型胆材。
- 当热门方原本可单选、但高质量 Dixon-Coles 模型给出明显更高的平局风险时，新增窄范围“强胆防平”双选保护。
- 复盘新增“预算压缩导致漏判”和“预算强制单选”诊断，单选命中率改按模型原始单选统计。
- 新增相关回归测试并更新 Android 调试安装包。
- Android“逐场预测”标题旁新增“一键查看 / 一键收起”，同时保留逐场独立展开和收起操作。

## 2026-08-06 球队中英文身份自动解析

- 扩充常见国内联赛简称到 FotMob 联赛的上下文映射；同联赛同开球时间只有一场时，可直接反推双方球队身份，无需预置俱乐部大全。
- 同时间存在多场候选时，新增 DBpedia 中英文足球俱乐部实体查询；只有双语主客队共同唯一指向同一场 FotMob 赛事时才保存别名和球队 ID。
- 双语查询结果和已确认的供应商球队 ID 写入本机缓存，后续分析、新闻、主流媒体和预测市场匹配共同复用。
- 未匹配诊断保存同联赛同时间候选数量和候选比赛；资料审计会逐项说明球队身份、xG、大小球、媒体及 Polymarket 缺失原因。
- 使用第 26101 期实测：球队实力覆盖由 6/14 提升到 14/14，xG 有效覆盖由 5/14 提升到 11/14，新闻匹配由 6/14 提升到 13/14。

## 2026-08-05 球队身份匹配持久化

- 新增本机 `team_identity.json` 身份库，唯一赛程确认的中英文别名和 FotMob / SofaScore 球队 ID 可跨分析、跨 App 升级复用。
- 球队名称归一化支持拉丁重音字符，收紧模糊包含匹配，避免成年队与 U20、女足仅因名称前缀相同而误配。
- FotMob / SofaScore 赛程匹配新增候选名称、置信度和失败原因诊断，并修复反向主客场候选取错球队 ID 的风险。
- 同一供应商球队 ID 只能归属一支本地球队，冲突关系不会写入身份库。
- Android 历史分析支持长按删除：期号卡片可删除整组，展开后的单条记录可单独删除；确认后同步清理归档文件并重建历史索引，复盘记录不受影响。
- 网络导致完整分析自动降级时，报告顶部及每场结论理由会明确列出未获得的信息来源，并说明这些资料未参与判断。

## 2026-08-02 完整分析证据与投注策略修正

- 手机端删除分析模式和样本场次控件，默认完整分析、样本固定 20 场；关键资料源大范围网络失败时自动降级为简单分析。
- 最高购彩金额移到原分析模式位置；标准分析报告隐藏风险和比分倾向，只展示置信度。
- 手机设置页新增 The Odds API Key 验证和零成本用量刷新，显示剩余、累计已用、本次查询消耗及当前开放项目数，并与本机引擎检测明确分开。
- 完整分析改用真实来源状态判断证据是否有效，默认中性数值不再冒充赛前情报。
- 调整实力与新浪情报的应用顺序，保留利好和不利的双向影响；伤停改为按位置与原因加权，并避免阵容缺席被重复扣分。
- 新浪欧赔改用逐公司去水后的概率共识，加入初盘到即盘变化和亚洲让球；外盘同时读取 1X2、让球和大小球，只有实际返回时才参与。
- 手机报告新增 The Odds API 调用审计，明确区分本次联网、5 分钟缓存、未匹配、Key 无效和额度耗尽，并显示匹配场数及额度响应头。
- 新闻、主流媒体和 Polymarket 明确标记为展示资料，不再通过标题关键词暗中改变概率。
- 资料审计区分确认无记录、请求失败、供应商错误和未匹配，并展示交锋、赔率变化、亚盘、大小球、媒体及预测市场状态。
- 新增单边已知时的保守球队别名学习，减少新球队在多个数据源中同时失配。
- 预算压缩由逐场贪心删项改为离散组合全局动态规划，在预算内最大化整张票覆盖概率。
- 每次分析和复盘保存独立赛前 JSON 快照，历史回测不再误用同一期最后一次采集的数据。
- 模型实验新增单选/双选/全包阈值的独立样本外门禁；概率权重和选项策略必须分别达标后才能晋级。

## 2026-07-26 14场胜平负模型增强

- 新增 SofaScore 缓存型 xG 补充源，受限时自动熔断并保留 FotMob 数据。
- Dixon-Coles 增加完整 xG、部分 xG、进失球分层估计和纯状态基线四档数据质量权重。
- 使用近期平局率动态校正低比分相关性，避免所有联赛共用同一平局参数。
- 修复 Android 历史分析丢失购彩截止时间；新报告会持久化期次信息，旧报告会从对应期号数据自动补回。
- 新增独立的纯胜平负实验层：赛前快照审计、按期走步回测、赔率与默认模型基线、Brier / Log Loss / ECE、平局与冷门召回、联赛与数据质量切片。
- 新增模型晋级门槛；旧数据只进入探索性回测，正式分析仅使用通过严格样本外回测后生成的 `active_model.json`。
- Android 本地复盘后自动刷新纯胜平负实验，未通过门槛时继续使用默认权重。

## 2026-06-22 首次 Git 上传

本次版本包含以下主要功能：

- 足球彩票 14 场胜平负分析与任选九取舍。
- 分析报告拆分为“胜平负预测”和“单场比分预测”两个顶部选项卡。
- 同一期的 14 场胜平负明细支持整期折叠，关键指标和任九摘要始终保留。
- 单场比分视图展示首选比分、两个备选比分及模型概率。
- 控制台新增手工单场预测：输入主队、客队即可获得比分、胜平负概率、置信度和风险；可选填欧洲赔率。
- 控制台新增飞书群机器人推送，分析报告、单场预测和赛后复盘生成后均可自动发送。
- 手工输入的对阵若存在于当前期数据中，会自动复用已采集的赔率、状态和伤停信号。
- 赛后复盘默认从新浪自动获取比分，手工 CSV 作为备用方案。
- 新增期号与逐场投注字段的双重校验，新浪页面或缓存串期时拒绝覆盖本地数据。
- HTML 历史中心、重大意外提示、比分 Top1/Top3 与任九命中统计。
- 历史中心按期号折叠，同一期展示全部查询结果，并在标题下标注查询时间。
- 完整单元测试覆盖主要采集、预测、报告、复盘与控制台流程。
