# Work Log / 工作记录 - 2026-07-04

## Branches / 分支

- `main`: 已合并并推送中国体彩网官方购彩截止时间支持。
  `main`: official Sporttery purchase deadline support has been merged and pushed.
- `ios`: 已由早期的 `mobile` 分支重命名，避免和 Android 开发混淆。
  `ios`: renamed from the earlier `mobile` branch to avoid confusing mobile platform work.
- `android`: 当前活跃的 Android App 开发分支。
  `android`: active Android app development branch.

## Android App Progress / Android App 进展

- 在 `android/FootballLotteryAndroid` 下创建 Android 项目。
  Created the Android project under `android/FootballLotteryAndroid`.
- 添加 Gradle Wrapper 文件，使项目可以通过命令行构建。
  Added Gradle wrapper files and made the project buildable from the command line.
- 使用 Kotlin、Jetpack Compose 和 Material 3 搭建 Android App 外壳。
  Added the Android app shell with Kotlin, Jetpack Compose, and Material 3.
- 添加底部导航，包含四个页面：分析、历史、单场、设置。
  Added bottom navigation with four screens: Analysis, History, Single match prediction, and Settings.
- 将分析页连接到 `/api/analysis`。
  Connected the Analysis screen to `/api/analysis`.
- 将历史页连接到 `/api/history`。
  Connected the History screen to `/api/history`.
- 将单场预测页连接到 `/api/single-prediction`。
  Connected the Single match prediction screen to `/api/single-prediction`.
- 将 Android App 切换为 Chaquopy 本机独立分析引擎模式。
  Switched the Android app to standalone local-engine mode with Chaquopy.
- 将现有 Python 分析包嵌入 Android debug 构建。
  Embedded the existing Python analysis package into the Android debug build.
- 添加 Android Python Bridge，用于本机分析、历史记录和单场预测。
  Added an Android Python bridge for local analysis, history, and single-match prediction.
- 移除手机 App 必须填写电脑后端地址的要求。
  Removed the phone-app requirement to enter a computer backend URL.
- 围绕嵌入式本机分析引擎重做设置页。
  Reworked Settings around the embedded local analysis engine.
- 在设置页添加本机引擎测试按钮。
  Added a local engine test in Settings.
- 为单场预测赔率添加本地校验：三项赔率必须全填或全空。
  Added local validation for single-match odds so odds are either fully filled or fully blank.
- 添加打开历史 HTML/Markdown 报告的入口，并修正相对路径解析。
  Added report-opening support for history entries and resolved relative report URLs on Android.
- 历史页首次打开时自动加载本机历史。
  Auto-loads report history the first time the History screen is opened.
- 添加 Android 设置文档：[README](../android/FootballLotteryAndroid/README.md)。
  Added Android setup documentation in `android/FootballLotteryAndroid/README.md`.

## Backend And API Support / 后端与 API 支持

- 添加适合移动端展示的分析结果序列化。
  Added mobile-friendly analysis payload serialization.
- 添加 `/api/history`，用于读取归档报告。
  Added `/api/history` for reading archived reports.
- 添加 `/api/health`，用于移动端轻量连接检查。
  Added `/api/health` for lightweight mobile connection checks.
- 添加 `football_lottery_agent.standalone_api`，支持手机本机分析流程。
  Added `football_lottery_agent.standalone_api` for phone-local analysis workflows.
- 保持现有 Web UI 和后端测试通过。
  Kept the existing web UI and backend test suite green.

## CI / 持续集成

- 添加 GitHub Actions 执行 Python 测试。
  Added GitHub Actions for Python tests.
- 添加 GitHub Actions 构建 Android debug APK。
  Added GitHub Actions for Android debug builds.
- 添加 debug APK 作为 GitHub Actions artifact 上传。
  Added debug APK upload as a GitHub Actions artifact.

## Verification / 验证

- Android `assembleDebug` 构建通过。
  Android debug build passed with `assembleDebug`.
- Python 测试通过：`74 passed`。
  Python test suite passed: `74 passed`.

## Evening Android Fixes / 晚间 Android 修复

- 验证本机 Windows Android 开发环境：
  Verified the local Android development environment on this Windows machine:
- Android Studio 路径：`D:\Program Files\Android\Android Studio`。
  Android Studio exists under `D:\Program Files\Android\Android Studio`.
- Android SDK 路径：`C:\Users\q3435\AppData\Local\Android\Sdk`。
  Android SDK exists under `C:\Users\q3435\AppData\Local\Android\Sdk`.
- 使用模拟器 `Medium_Phone_API_36.1` 进行测试。
  Emulator `Medium_Phone_API_36.1` was available and used for testing.
- 使用 `adb` 将 debug APK 安装到模拟器。
  Installed the debug APK into the emulator with `adb`.
- 确认嵌入式 Chaquopy/Python 本机引擎可以在 Android App 内启动。
  Confirmed the embedded Chaquopy/Python local engine starts inside the Android app.
- 确认设置页可以运行本机引擎健康检查。
  Confirmed Settings can run the local engine health check.
- 确认单场预测可以完全在手机本机运行。
  Confirmed single-match prediction works fully on-device.
- 通过快速移动端模式修复独立 Android 分析过慢的问题：
  Fixed standalone Android analysis being too slow by adding a fast mobile mode:
- 快速模式跳过新闻/上下文抓取、FotMob 实力模型、完整 Sina 伤停/情报/交锋详情。
  The fast mode skips news/context fetches, the FotMob strength model, and full Sina injury/intelligence/history detail fetches.
- 通过 Sina 赔率轻量采集修复快速模式数据过弱的问题：
  Fixed the fast mode being too weak by adding Sina odds-only collection.
- 保留轻量 `footballMatchOddsEuro` 欧赔采集，避免每场都回退到默认占位赔率。
  Kept lightweight `footballMatchOddsEuro` odds collection to avoid falling back to default placeholder odds for every match.
- 修复所有推荐都变成 `3/1/0` 的问题。
  Prevented every recommendation from becoming `3/1/0`.
- 在模拟器中验证 `26090` 期：
  Verified issue `26090` in the emulator.
- 报告生成成功，摘要显示 `3` 个单选、`2` 个低风险。
  The report generated successfully, and the summary showed `3` singles and `2` low-risk picks.
- 推荐结果出现 `0/1`、单选 `0`、`3/1`、`0/3`，不再全部是 `3/1/0`。
  Examples included `0/1`, single `0`, `3/1`, and `0/3`, not all `3/1/0`.
- 让比赛卡片明确显示购彩建议，例如：`建议选择：主胜 / 平 / 客胜（3/1/0）`。
  Made match cards clearer by displaying explicit purchase recommendations, such as `建议选择：主胜 / 平 / 客胜（3/1/0）`.
- 增加置信度和风险文本展示。
  Added confidence and risk text.
- 修复历史页交互：
  Fixed the History screen interaction.
- 点击“历史”会刷新本机历史；点击历史条目会选中该报告；选中报告会显示详情卡片。
  Tapping History refreshes local history; tapping a history item selects it; the selected item is shown in a detail card.
- 修复独立 Android 模式下历史报告无法真正打开的问题：
  Fixed local history reports not really opening in standalone Android mode.
- 独立历史 API 现在返回 Markdown 报告正文。
  The standalone history API now returns Markdown report text.
- Android 历史详情页在 App 内展示完整报告正文。
  Android History detail displays the full report body in-app.
- 本机文件打开按钮仅在历史记录是 Web URL 时显示，避免点击无反应。
  Local file open buttons are hidden unless the entry is a real web URL.
- 在模拟器中确认历史页可以显示 `26090` 的完整 Markdown 报告正文，包括报告标题、任九建议、14 场逐场建议和详细理由。
  Confirmed in the emulator that the History screen displays the full Markdown report body for `26090`, including the report title, Choose 9 advice, 14-match recommendations, and detailed reasons.

## Latest Android Commits / 最新 Android 提交

- `c0b6c4e Use odds in standalone Android reports`
- `dd14c41 Clarify Android picks and history selection`
- `c596e4b Speed up standalone Android analysis`
- `96bda5e Embed standalone analysis engine in Android app`
- `0878ef8 Add one-click mobile backend launcher`
- `2a2f3ca Update Android work log`
- `255fca6 Upload Android debug APK from CI`
- `e7a6a9a Validate Android backend URL before requests`
- `d94ed41 Resolve Android history report URLs`
- `fa5f10c Fix Android CI Gradle wrapper permissions`
- `613af69 Add CI for backend and Android`
- `2d38680 Add backend health check for Android`
- `0fed5a4 Auto-load Android history screen`
- `a2cf5b9 Validate Android single prediction odds`
- `846334a Add Android backend connection test`
- `b6c4462 Document Android app setup`

## Next Suggested Work / 下一步建议

- 将独立 debug APK 安装到真实 Android 手机上测试。
  Install the standalone debug APK on a physical Android phone.
- 在手机移动网络或 Wi-Fi 下测试当前期号分析。
  Test current-issue analysis on the phone with mobile network or Wi-Fi enabled.
- 为分析结果添加更丰富的比赛详情页。
  Add richer match detail views for analysis predictions.
- 优化 Android UI 的加载态、空状态和长报告阅读体验。
  Add loading and empty states with more polished Android UI behavior.
- 将较大的 `MainActivity.kt` 拆分为 model、API、ViewModel 和 screen 文件。
  Split the large Android `MainActivity.kt` into smaller model, API, view model, and screen files.
