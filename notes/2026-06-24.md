# 2026-06-24 工作记录

## 今日完成

- 修复控制台按钮点击后无反应的问题：页面脚本里的换行转义错误会导致浏览器 JS 语法错误，现已修正。
- 增加前端必填校验：主队、客队、期号、xG 样本场次、复盘 CSV 等漏填时会在页面直接提示并聚焦输入框。
- 增强后端输入校验：空值、xG 越界、欧赔填写不完整、复盘数据路径缺失等会返回明确错误。
- 将赛后复盘改成默认全自动：自动使用 `data/collected_issue.json`，自动拉取赛果；手工 CSV 仅作为备用。
- 将赛果来源从单一新浪扩展为多来源 fallback：新浪体育、东方财富开奖、东方财富历史开奖、500彩票网开奖。
- 支持“仅彩果”备用源：备用源没有精确比分时，仍可完成胜平负和任九复盘；比分 Top1/Top3 显示为 N/A，避免误算。
- Web UI 成功消息会显示实际采用的赛果来源，失败时会提示缺少哪些场次或来源暂不可用。
- CLI 自动复盘也改为使用同一套多来源赛果逻辑。
- 补充回归测试覆盖：按钮脚本转义、必填校验、多来源 fallback、仅彩果复盘、CLI 自动复盘。

## 验证结果

- `python -m unittest tests.test_web_ui` 通过。
- `python -m unittest tests.test_review tests.test_web_ui tests.test_cli` 通过。
- `python -m unittest discover -s tests` 通过，共 47 项。
- 本机实测 `26087` 自动赛果来源为新浪体育，14 场精确比分可用。

## 注意事项

- 当前仓库未配置 Git remote，无法直接推送到远端仓库。
- 如果控制台已经打开，需要重启 `start_ui.bat` 或刷新服务，才能加载最新代码。
