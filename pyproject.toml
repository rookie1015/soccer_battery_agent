[project]
name = "football-lottery-agent"
version = "0.5.6"
description = "Football lottery analysis assistant for 14-match and choose-9 SFC tickets."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
football-lottery-agent = "football_lottery_agent.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
